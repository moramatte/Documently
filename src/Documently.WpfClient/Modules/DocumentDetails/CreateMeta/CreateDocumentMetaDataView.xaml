<UserControl x:Class="Documently.WpfClient.Modules.DocumentDetails.CreateMeta.CreateDocumentMetaDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">
    <Grid>
        <StackPanel>
        <StackPanel>
            <Label Foreground="Black" Content="Title: "  />
            <TextBox Text="{Binding Command.Title, UpdateSourceTrigger=PropertyChanged}" Width="400" />
        </StackPanel>
            <Button Width="100" Content="Save" x:Name="Save" HorizontalAlignment="Right" />
        </StackPanel>
    </Grid>
</UserControl>
